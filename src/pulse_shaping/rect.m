function pulse = rect(n_os)
%RECT Generates a rectangular pulse for the specified oversampling factor.
%
% Inputs:
%   n_os               - Oversampling factor.
%
% Outputs:
%   pulse              - Rectangular pulse samples.
%
%%RECT Provides the rectangular pulse with oversampling factor n_os

%% of Tuna Öztürk and Kerem Kaya
pulse = ones(1,n_os);

end

